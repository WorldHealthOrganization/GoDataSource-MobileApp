diff --git a/node_modules/react-native-material-ripple/index.js b/node_modules/react-native-material-ripple/index.js
index 9df9b50..a6725ce 100644
--- a/node_modules/react-native-material-ripple/index.js
+++ b/node_modules/react-native-material-ripple/index.js
@@ -139,6 +139,12 @@ export default class Ripple extends PureComponent {
       { locationX: w2, locationY: h2 }:
       event.nativeEvent;
 
+    // Guard against missing touch coordinates (e.g. accessibility-triggered
+    // presses or taps that land in the hitSlop region without nativeEvent
+    // location data) which otherwise produce NaN and crash native rendering.
+    if (!Number.isFinite(locationX)) locationX = w2;
+    if (!Number.isFinite(locationY)) locationY = h2;
+
     let offsetX = Math.abs(w2 - locationX);
     let offsetY = Math.abs(h2 - locationY);
 
@@ -146,6 +152,8 @@ export default class Ripple extends PureComponent {
       0.5 * rippleSize:
       Math.sqrt(Math.pow(w2 + offsetX, 2) + Math.pow(h2 + offsetY, 2));
 
+    if (!Number.isFinite(R) || R <= 0) R = radius;
+
     let ripple = {
       unique: this.unique++,
       progress: new Animated.Value(0),
