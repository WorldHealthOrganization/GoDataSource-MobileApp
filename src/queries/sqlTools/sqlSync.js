import constants from './constants';

// The main method that is called to handle the sync for the data that can be filtered/sorted/grouped
export function sqlSync(filesPath) {
    return new Promise((resolve, reject) => {

    })
}